# React Practical

## The tasks of the topic JSX:
Change file *./src/components/Info/index.jsx*.

Function `Info` must return JSX `div` element with:

- text inside : `JSX produces React 'elements'`
- attribute `data-testid` with value `element-info`